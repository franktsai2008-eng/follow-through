# 9/11 五 Build with YOU hackathon 作戰計畫（Civic Hall，124 E 14th St）

一句話：今天不是去比賽，是做三件事——把 A2 實驗的十組結果帶進場給對的人看、問十個人同一句話、13:30 前先交一版能交的東西。獎品是副產品。

## 0. 今晚已經做好的（全部在 `~/projects/a2-harness/`）
- **harness v0 跑完十組 baseline**（買方 Claude、賣方 GPT，純文字，十回合上限，事後盲判）。結果 `results/baseline-2026-09-11.md`：
  - 十組五組雙方都記成交：有合法解的四組全成交，六組陷阱組只成一組。
  - 幽靈成交 0。五筆成交四筆紀錄一模一樣，第五筆（G08）同一筆交易一邊記「箱」一邊記「令」，機器對帳會判爭議。
  - **賣方十組零越權**（盲判），十組有八組主動講「要主管批」，五次走人全是賣方。
  - **唯一越線的是買方**：G05 卡片寫 30 天內到貨、遲一天罰 $800，它簽了第 40 天還記成沒問題。
- **learn 模式也在家跑完了**（G02→G10 再 G01 讀九條課），對照表 `results/compare-baseline-2026-09-11-vs-learn-home.md`。盲判：有課的賣方十組全部主動講「要主管批」（冷開是八組）、仍然零越權、成交 4/10（冷開 5/10）——少掉的那一組是 G05，有課之後賣方第二回合就走人，不再讓買方簽那筆遲十天的爛單。學習的方向是「更守線、更早講限制」，不是「更會成交」，demo 就這樣講。
- 英文講稿 `SCRIPT-EN.md`（slopmonster 5/5）、田野表 `FIELD-NOTES.md`（含四位贊助商已查證的 hook 與 You.com API 端點）、demo 頁 `demo/index.html`（impeccable-detect 0 findings）。
- 本機 git 已 init 一個 commit。**public repo 還沒建，等你回 1A**（見最後）。
- 兩位獨立審查的結論已吸收：demo 敘事改成真實結果（不是「賣方偷答應 200 件」，那件事沒發生過）、開場改 90 秒實驗優先、ask 改成零成本、身分句改 F-1 安全版。

## 1. 分鐘表
| 時間 | 做什麼 |
|---|---|
| 09:20 前 | 車上先開 Luma 活動頁與信箱找 partner code 連結，不等櫃檯。手機鎖屏換成 LinkedIn QR。 |
| 08:30–09:20 | PHIL，Keating 208。下課直接走，不回宿舍。 |
| 09:22–10:20 | 步行到 Fordham Metro-North（10 分）。**上車前看 TrainTime，Harlem 線這個時段約 30 分一班，錯過一班就是 10:50 到。** Grand Central 下車走 **4/5 快車**到 14 St–Union Sq（不要搭 6 號慢車），東側出口，124 E 14th St 就在 Union Square East。 |
| 10:20 | 報到掃 Luma QR。櫃檯問四件：partner codes、Wi-Fi、Tom Haddock 全天在還是決賽才來、交件後能不能改。拿早餐。 |
| 10:20–11:30 | workshop 後半。只記兩件：You.com key 怎麼拿、Daytona 怎麼跑一支 script。 |
| 11:30–11:45 | 開工前先講人。Tom 在就先找 Tom（§3），不在就找 You.com 工程師問同一句。 |
| 11:45–13:30 | 建置第一段（§2 的 1→2）。 |
| **13:30** | **先交最小可交件**：repo 連結＋demo 頁截圖＋三句描述。之後全是升級不是賭。 |
| 13:30–14:15 | 午餐＝問人，目標五個。手機開 `results/baseline-2026-09-11.md` 截圖。 |
| 14:15–16:00 | 建置第二段（§2 的 3→4），錄 60 秒。**14:00 落後就照 §2 砍單。** |
| 16:15 | 最終版交件，不壓 16:30。 |
| 16:30–17:10 | 評審審件＝第二波問人（再五個），找 Jacob Rissman、Vedran Jukic。 |
| 17:10–18:00 | 前五名 demo。填 FIELD-NOTES「前五名共同點」「評審獎勵什麼」。 |
| 18:00 後 | 留到人散。**離場兩小時內寄 follow-up 信**，不是下週。 |

## 2. 場內建置清單（11:45 改版：主辦說四家都要用；骨架全在 repo，You.com 與 CrewAI 已真跑，Daytona 與 One 只差 key）
| # | 家 | 在 harness 裡的位置 | 狀態 | 到場要做的 |
|---|---|---|---|---|
| 1 | You.com | `--grounded`：每組一行公開市價塞進兩張卡（`grounding.py`，免 key 走 `api.you.com/mcp?profile=free`，100 次/天；有 key 自動改 Search API） | G01/G05/G07 已跑 `runs/grounded-2026-09-11` | 只剩 judge＋score＋demo 頁重建（指令在下面）。拿到 key 就 `export YDC_API_KEY=...`，不拿也成立 |
| 2 | CrewAI | `audit_crew.py`：盲判改成一個 auditor agent 的 crew，LLM 是自訂 BaseLLM 轉 `claude -p`，零 key；輸出 `crew_judge_*.json` 跟原 judge 同 schema，`--compare` 出一致率 | G01 seller 已跑，3/3 一致 | `.venv/bin/python audit_crew.py --run baseline-2026-09-11 --groups G01 G05 G07` 再 `--compare`（每組兩側約 20 秒） |
| 3 | Daytona | `sandbox_daytona.py`：把 score.py＋紀錄 JSON 丟進沙盒重算表，零登入零 key，寫 `daytona_replay.md`（含 sandbox id） | 乾跑通過，21 檔 49KB | 櫃台拿 `DAYTONA_API_KEY`，跑 `DAYTONA_API_KEY=... .venv/bin/python sandbox_daytona.py --run baseline-2026-09-11 --groups G01 G05 G07` |
| 4 | One | `receipt_one.py`：每筆成交寫收據（雙方紀錄＋盲判＋transcript sha256），經 One 的 MCP 寄出，harness 不碰信箱 token；MCP client 就是 `claude -p` | 乾跑通過，G01 收據已生成 | 櫃台拿 `ONE_SECRET`（app.withone.ai → API keys）並在那邊連 Gmail；`ONE_SECRET=... ONE_RECEIPT_TO=<你自己的信箱> python3 receipt_one.py --run baseline-2026-09-11 --groups G01`。**只寄給自己**。 |

**跑完 grounded 後的三行**：
```
python3 judge.py --run grounded-2026-09-11 && python3 score.py --run grounded-2026-09-11
.venv/bin/python audit_crew.py --run grounded-2026-09-11
python3 build_demo.py --baseline baseline-2026-09-11 --learn learn-home --grounded grounded-2026-09-11
```
demo 頁「Four tools, one job each」那格是讀檔案的：哪家沒跑就寫 not run yet，不會假裝。

砍單順序（14:00 落後）：One → Daytona → CrewAI 只留 G01 一組 → You.com 三組永不砍（已經跑完）。learn 對照與交件永不砍。
**說法一句**：CrewAI 管單一主人的編排（稽核），One 管接工具，Daytona 管誰都不控制的重算，You.com 管公開事實；兩家公司的 agent 誰能承諾什麼，四家中間沒人管，那就是這個實驗。

## 3. 跟 Tom Haddock 怎麼講（逐字在 SCRIPT-EN.md）
**目標不是讓他同意你的方向，是讓他用客戶現場看到的東西回答，然後指一個人給你。** 手段是「我跑了十組，結果反直覺，你告訴我哪裡不對」。

1. 90 秒開場（實驗先講，身分不講）→ 直接問那一句：「上一次你的客戶要讓 agent 跟公司外面的東西講話，legal 或 ops 逼他們先加什麼才准上線？」
2. 聽。原話記下來，不改寫。
3. 他傾身了才拿出數字（手機截圖）。
4. Ask 只有一個、零成本：「這房間裡你會指誰給我問這題？我自己去問。」他熱絡再加一句「跑到五十組寄你一頁，不用回」。15 分鐘 call 與客戶 intro 一律留到 write-up 寄出之後。
5. 他問「產品是什麼」→「還沒有，是實驗不是公司，我想知道缺的是授權還是紀錄」，停。台灣報關那句只在他追問才講。

**不說的詞**：platform、integration layer、neutral、every company in the world、meeting。**不說「我有一間 AI 工作室」**——在美國公開場合對美國公司主管講在美經營事業，等於自造紀錄，也會直接招來「你接不接客戶」。統一用：「來之前在台灣幫小公司做自動化，這是學生實驗，不賣東西。」
**他若說「不需要新層，email 加 LLM 就夠」**：那是 A2 的證據，不辯，說謝謝，記下來。

同一句話問另外三位（SCRIPT-EN.md 各有 hook）：Jacob Rissman（One，授權層，他 9/2 貼文原話 "the harder test starts when they have to authenticate"）、Vedran Jukic（Daytona，隔離執行）、Michael Munson（Clean Data Alliance，紀錄層）。同一題四個人答案分歧，本身就是資料。

## 4. 說服的五條原則
1. 帶證據不帶願景。你手上有別人沒有的：十組真跑出來的 transcript、盲判、四個數字。
2. 問他能從經驗回答的問題，不是請他評價你的點子。
3. Ask 小到他不用想。
4. 先講實驗再講身分。先報大一他會切成 mentor 模式，你要的是同行回答。
5. 不爭辯。反對意見是最貴的資料。

## 5. 對其他 builders
25 字版問題＋「never」時的救援問（FIELD-NOTES 第一段）。名牌上手寫 "cross-company agents"，讓人來問你。
有人來組隊：只在對方接受「今天做這個 demo」時加入，否則單飛。

## 6. 60 秒 demo（≤110 字，SCRIPT-EN 最後一段）
講真實結果：賣方零越權、走人代替越權、紀錄一致但單位不同、越線的是買方。learn 對照用「成交數／回合數／是否仍零越權」三個可畫成線的數字，不是戲。

## 7. 規則與備案
- 場內口頭回饋不進記分板、不進 M23。不錄音。
- Credits 獎品直接收。**要 SSN／W-9／tax ID 的表，當場說 "I'll need to check with my school's international office first"，不簽。**
- Wi-Fi 爛：手機熱點，先確認流量。harness 只靠 CLI 連網。
- codex 配額耗盡：`--swap` 換邊，或兩邊都 Claude（結果標 degraded，不進 baseline）。
- Tom 不在場：他是 featured guest，決賽 17:10 一定在。
- 帶：充電線＋小延長線、離線的 results 截圖、repo 在手機上開得起來。吃飯。

## 8. 明天以後
- 9/12 六：AI Tinkerers 仍 pending → 中午前沒核准就去 Solo Unicorn Mixer 14:00（315 W 39th St）。
- 9/12 23:59：interco panel 五題決策卡期限（proposed，不回等於維持提案，我不重問）。
- 場內 FIELD-NOTES 與 results 我隔天整併進金庫，跨模型獨立評分再排一次；harness v1 改成 prose memo 加盲抽取（程式已寫好，`--record-mode memo` 已是預設）。
