# 9/11 五 Build with YOU hackathon 作戰計畫（Civic Hall，124 E 14th St）

一句話：今天不是去比賽，是去做三件事——把 A2 實驗的數字帶進場給對的人看、問十個人同一句話、交一個 60 秒 demo。獎品是副產品。

## 0. 今晚已經做好的
- `~/projects/a2-harness/`：兩家公司的 AI 用純文字談判的 harness v0。買方 Claude、賣方 GPT，十組參數，四個數字（成交率、紀錄一致率、越權率、自報率）。
- 十組 baseline 已跑，結果在 `results/baseline-2026-09-11.md`。數字：[跑完填]
- 英文講稿 `SCRIPT-EN.md`（slopmonster 5/5），田野筆記表 `FIELD-NOTES.md`。
- 場內要做的清單在 §2，優先序已排。

## 1. 分鐘表
| 時間 | 做什麼 |
|---|---|
| 08:30–09:20 | PHIL，Keating 208。下課直接走。 |
| 09:22–10:20 | Keating → Fordham Metro-North 站（步行 10 分）→ 9:40 左右班次到 Grand Central（約 20 分）→ 6 號線下城到 14 St–Union Sq（8 分）→ 東側出口，124 E 14th St 就在 Union Square East。備案：D 線 Fordham Rd → 34 St 轉 N/Q/R 到 Union Sq，便宜但約 50 分。 |
| 10:20 | 報到掃 Luma QR（信箱 .pkpass）。**當場問三件事**：partner codes（You.com key、Daytona、CrewAI、One）、Wi-Fi、Tom Haddock 今天幾點在。拿早餐。 |
| 10:20–11:30 | workshop 後半。坐贊助商工程師附近。要記的只有兩件：You.com API 真正的 endpoint 與 auth header、Daytona 怎麼跑一支 python script。 |
| 11:30–11:45 | 開工前先講人。Tom 在就先找 Tom（§3），不在就找任一 You.com 工程師問 API，順便問 §3 那一句。 |
| 11:45–13:30 | 建置第一段（§2 的 1→2→3）。`--learn` demo run 先丟背景跑。 |
| 13:30–14:15 | 午餐＝問人時段。目標五個人。手機開 `results/…md` 截圖，比空口有效。 |
| 14:15–16:00 | 建置第二段（§2 的 4）。錄 60 秒 demo，寫交件描述。 |
| 16:00–16:15 | **提早 15 分鐘交件**。16:30 截止不要壓線。 |
| 16:30–17:10 | 評審審件時段＝第二波問人（再五個），找 Jacob Rissman、Vedran Jukic。 |
| 17:10–18:00 | 前五名 demo。記 FIELD-NOTES 的「前五名共同點」「評審獎勵什麼」。 |
| 18:00 後 | essay 已交，不用趕。留到人散，最後找還沒問到的贊助商。 |

## 2. 場內建置清單（照順序，做不完就停）
1. **`--learn` demo run**（不需要任何 key）：`python3 harness.py --all --learn --tag learn-venue`。跑完 `python3 score.py --run learn-venue`。這是評審要看的「會學習」證據：G01 冷開 vs 九課之後。
2. **You.com grounding**（要 key）：跑之前用 Search API 查「<product> wholesale price per unit 2026」，把一行 market reference 塞進兩張卡，再跑十組 `--tag grounded`。比較 overreach 有沒有動。endpoint 照 workshop 講的寫，不要猜。
3. **Daytona**：把 harness 丟進 sandbox 跑一組，截圖。故事：兩家公司的 agent 各自在隔離盒子裡跑，誰都不用執行對方的 code。
4. **demo 頁**：單檔 HTML 讀 `results/*.json`，左右並排 G01 round 3 冷開 vs 有課，下面四個數字表。不做花的。
5. CrewAI wrapper：有剩時間才做。CLI relay 才是儀器。

交件包：GitHub repo（public，scenarios 全合成、沒有 PII，確認 `runs/` 裡沒有帳號資訊）、60 秒 demo、三句描述。標題建議：Two companies' agents, one plain-text deal: who overreached, and who admitted it.

## 3. 跟 Tom Haddock 怎麼講（逐字在 SCRIPT-EN.md）
**說服的目標不是讓他同意你的方向。** 是讓他 (a) 用他每天在客戶現場看到的東西回答 A2 那一題，(b) 答應一個小的下一步。手段是「我已經跑了實驗，數字在這，你告訴我哪裡錯」，不是 pitch。

流程：
1. 30 秒開場（誰、在做什麼、昨晚跑了十組）→ 直接問那一句。
2. 聽。他的原話記下來，不改寫。
3. 他傾身了才拿出數字（手機截圖 `results/…md`）："Does that match what you see in production?"
4. 小 ask：下週寄 write-up 換 15 分鐘 call；有客戶在做跨公司 agent 就求一個 intro。講明「不是在賣東西」。
5. 他問「所以產品是什麼」→ 「還不知道，第一個測試場是台灣報關行催貨主補件」，就這樣，不多講。

**不要說的詞**：platform、integration layer、neutral、every company in the world、meeting。這些是 panel 判死的字面。
**他若說「不需要新層，email 加 LLM 就夠」**：那是 A2 的證據，不要辯，說謝謝，記下來。

同一句話問另外三位，各換一個切角（SCRIPT-EN.md 有寫）：Jacob Rissman（One，授權層）、Vedran Jukic（Daytona，執行隔離）、Michael Munson（Clean Data Alliance，紀錄層）。這四個人剛好各坐在 A2「缺的那一層」的四個候選上，同一題問四個人，答案分歧本身就是資料。

## 4. 說服的五條原則
1. 帶證據不帶願景。你手上有別人沒有的東西：十組真實跑出來的 transcript 與四個數字。
2. 問他能從經驗回答的問題，不是請他評價你的想法。人對「你怎麼看我的點子」會客套，對「你客戶現場哪裡壞」會講真話。
3. Ask 要小到他不用想就能答應。
4. 你的身分是資產：18 歲、大一、昨晚跑了十組。好記，沒威脅。不要藏。
5. 不爭辯。反對意見是最貴的資料。

## 5. 對其他 builders（十個人）
開場一句＋問題一句（FIELD-NOTES 第一段）。時機：workshop 中場、午餐、排隊、評審時段。答案按 T/A/R/N 記（transport／authorization／record／從沒發生過）。
有人來組隊：只在對方接受「今天做 A2 harness 的 demo」時加入，否則單飛。

## 6. 60 秒 demo 腳本
0–10s 問題一句 → 10–35s 畫面：G01 冷開 vs 有課，round 3 賣方一邊直接承諾沒有的 200 件、一邊說 subject to my director's approval → 35–50s 四個數字表 → 50–60s 下一步：You.com grounding 之後 overreach 從 Z 變 Z′，接下來 50 組加 swap。

## 7. 規則與備案
- 場內口頭回饋不進記分板、不進 M23。不錄音。
- Credits 獎品直接收；要簽稅表的先問我。
- Wi-Fi 爛：開手機熱點，harness 只靠 CLI 連網。
- codex 配額耗盡：`--swap` 換邊，或兩邊都 Claude（結果標 degraded，不進 baseline）。
- Tom 不在場：他是 featured guest，決賽 17:10 一定在。先問 You.com 的人他幾點到。
- 有人問你公司做什麼：真實現況——「一人 AI 工作室，做過台灣中小企業的自動流，現在在大一，正在測跨公司 agent 這題」。

## 8. 明天以後
- 9/12 六：AI Tinkerers 仍 pending → 中午前沒核准就去 Solo Unicorn Mixer 14:00（315 W 39th St）。
- 9/12 23:59：interco panel 五題決策卡期限（proposed，不回等於維持提案，我不重問）。
- 場內 FIELD-NOTES 與 results 我隔天整併進金庫，跨模型獨立評分再排一次。
